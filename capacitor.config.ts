import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.primkopau.loyaltyapp',
  appName: 'Airforce Mart SDM',
  webDir: 'dist',
};

export default config;
